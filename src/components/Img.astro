---
import renderImg from "../api/renderImg.js";
import type { ImgConfigOptions, ImgHTMLData } from "../types.d";

const { link, style, img }: ImgHTMLData = await renderImg(
  Astro.props as ImgConfigOptions
);
---

<Fragment set:html={link + style + img} />
