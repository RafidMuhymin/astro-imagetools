---
import renderImage from "../api/renderImage.js";
import type { PictureConfigOptions, ImageHTMLData } from "../types.d";

const { image, link, style }: ImageHTMLData = await renderImage(
  Astro.props as PictureConfigOptions
);
---

<Fragment set:html={link + style + image} />
