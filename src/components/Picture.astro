---
import renderPicture from "../api/renderPicture.js";
import type { PictureConfigOptions, PictureHTMLData } from "../types.d";

const { picture, link, style }: PictureHTMLData = await renderPicture(
  Astro.props as PictureConfigOptions
);
---

<Fragment set:html={link + style + picture} />
