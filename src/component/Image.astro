---
import type { ImageConfig as Props } from "../types.d.ts";
import renderImage from "./utils/renderImage.js";

const { image, link, styling } = await renderImage(Astro.props as Props);
---

<Fragment set:html={link + styling + image} />
