---
import type { ImageConfig as Props } from "../types.d.ts";
import renderImage from "./utils/renderImage.js";

const { image, link, srcStyle, style, class } = await renderImage(Astro.props as Props);
---

<Fragment set:html={link + srcStyle + image} />
