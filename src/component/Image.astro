---
import astroConfig from "/astro.config";
import renderImage from "./utils/renderImage.js";

const { image, link, style } = await renderImage(Astro.props, astroConfig);
---

<Fragment set:html={link + style + image} />
