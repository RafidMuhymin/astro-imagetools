---
import renderImg from "../api/renderImg.js";
import type { ImgConfigOptions } from "../types.d";

const { link, style, img } = await renderImg(Astro.props as ImgConfigOptions);
---

<Fragment set:html={link + style + img} />
